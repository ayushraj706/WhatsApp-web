name: Render Keep Alive
on:
  schedule:
    - cron: '*/15 * * * *' # Har 15 minute mein chalega
  workflow_dispatch: # Taaki aap manually bhi chala sakein

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -s https://whatsapp-web-2-in-one.onrender.com > /dev/null
        
